version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_install:
      - export PY_ALSA_MIDI_NO_COMPILE=1

sphinx:
   configuration: docs/conf.py

python:
   install:
   - method: pip
     path: .
   - requirements: docs/requirements.txt
